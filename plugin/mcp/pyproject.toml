[project]
name = "obwiki-mcp"
version = "0.1.0"
description = "ObWiki MCP Server — AI agent tool surface for knowledge base operations"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.8.0",
    "httpx>=0.28.0",
]

[project.scripts]
obwiki-mcp = "server:main"
